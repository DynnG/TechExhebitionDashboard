# UI/UX Implementation Plan

## Navigation
- Change the background color back to sea salt, and choose a contrasting text color that works best with it..

## Dashboard
- Remove the AI Scraper Engine button.
- On the Coverage gap assessment card remove the run scraper button.
- Fix the bento style format especially the Events by region card because it is not aligned to Exhibition distribution by month card.
- In the Exhibition distribution by month calendar view: replace the redundant text entries with a single icon. Additionally, add clear 'From' and 'To' labels so users can easily distinguish the date range.
- On the Coverage gap assessment card, make sure the events is synced to it especially the sub cards it will reflect from the events. Also remove the Automated scraper pipeline active text and the logo beside it.
- On the Recently added exhibitions card remove the +Add new record button 
- In the AI discovery & scraper engine status must be synced to the scraper engine that every run there will be reflected on here
- Note: Reference the color scheme in the markdown file and incorporate the brand green into the design. In particular, swap out the current yellow accents on the scoreboard for this green.

## Events
- Remove the dot high indicator beside the fit score and its group because it is redundant just maintain the fit score and below of it the fit score indicator like 4 and below is HIGH.
- Also when you click a certain event inside of it there is two score indicator remove one of it. But make sure the fit score is always the last on the top right.

## Scraper Engine
- Remove the Internal Crawler & Scheduler button and Apify + Google Gemini Engine(Batch 11 Spec) because we don’t need switching button anymore just the container of Tech exhibition discovery engine (Batch 11).
- On the  Tech exhibition discovery engine (Batch 11) card can you remove the (Batch 11) on the text, remove the tag which is this and its icon “Real-Time Stream · Apify + Gemini 2.5 Flash”, rename the Start discover & crawl button into “Start Scraping”

## Queues
- Make the Queue empty card indicator that is Queue is clear context lots bigger or fix the design of it, it looks so ugly not aesthetic and elegant.
- Remove the Corrections Queue (Data Edits) button and its context.

## History
- Fix the design of it, Queue Decision and Attended Exhibitions Log similar to each other especially the details format.

## Settings
- Reformat it on how you design go wild as you can.

## Login
- Make the Lifewood logo on the on top left on its card.
- Make the page responsive website design so that in the laptop resolution doesn’t need to scroll.
- Remove the forgot password

## Overall
- Make the design and color scheme consistently, and well balanced.
